name: CI-CD

on: 
  push:
    brannches: ["main"]
    
jobs:
  CI:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3.0.1
      - name: Construção da imagem Docker
        uses: docker/build-push-action@v3.2.0
        with:
          context: ./src
          file: ./src/Dockerfile
          push: true
          tags:
            ellisonalencar/kube-news:v1
            ellisonalencar/kube-news:latest
  
